#!/usr/bin/env python3


def add(a: float, b: float) -> float:
    """
        add two numbers
        Arguments:
            a: number
            b: number
        Returns:
            sum of two numbers
    """
    return float(a + b)
